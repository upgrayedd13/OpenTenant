services:
    app:
        environment:
            FLASK_DEBUG: 1
        ports:
            - "8000:8000"

    opentenant-nginx:
        networks: !reset
            - default

    db:
        ports:
            - "5432:5432"
